# Base URL of your backend API
REACT_APP_API_BASE_URL=http://localhost:5000/api

# Note: the Razorpay Key ID does NOT need to be duplicated here.
# The backend returns it in the create-order response, which keeps
# the single source of truth server-side. Never put RAZORPAY_KEY_SECRET
# anywhere in the frontend.
